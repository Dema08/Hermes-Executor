<UserControl x:Class="Hermes_Executor.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Border Background="{DynamicResource BackgroundLight}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="10,0">
                <TextBlock Text="CONSOLE" Foreground="{DynamicResource HermesGold}" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button x:Name="BtnClearConsole" Content="🗑️" Width="26" Height="26" HorizontalAlignment="Right" Background="Transparent" Foreground="{DynamicResource TextPrimary}" BorderThickness="0"/>
            </Grid>

            <TextBox Grid.Row="1" x:Name="TxtConsoleOutput" Background="{DynamicResource ConsoleBackground}" Foreground="{DynamicResource SuccessGreen}" FontFamily="Consolas" FontSize="12" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" BorderThickness="0" Margin="10,0"/>

            <Grid Grid.Row="2" Margin="10">
                <TextBox x:Name="TxtConsoleInput" Background="{DynamicResource InputBackground}" Foreground="{DynamicResource TextPrimary}" FontFamily="Consolas" FontSize="12" Padding="5" BorderBrush="{DynamicResource BorderBrush}"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
